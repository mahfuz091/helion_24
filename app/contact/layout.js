import React from "react";
import "./contact.css";
export const metadata = {
  title: "Helion - Contact us",
  description: "Helion",
};

const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
