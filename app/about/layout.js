import React from "react";
import "./about.css";
export const metadata = {
  title: "Helion - About us",
  description: "Helion",
};

const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
